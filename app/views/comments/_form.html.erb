<%= form_with(model: @comment, local: true) do |form| %>

  <div class="field">
    <%= form.text_area :commentText %>
  </div>
  
  <%= hidden_field :Contributions_id, value: @contribution.id  %>

  <div class="actions">
    <%= form.submit %>
  </div>
  
  
<% end %>
